# Its-my-bash

Its-my-bash is a ultra lighweight bash shell customization, it does not require anything more than a
freshly installed linux from any distribution. You simply need to follow the [installation guideline](Install)

## Install

```git clone https://github.com/Alexander7474/Its-my-bash.git ~/.bash && echo "source ~/.bash/itsmybash" >> ~/.bashrc```
